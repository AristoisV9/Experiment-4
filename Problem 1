#include<iostream>

using namespace std;

int main()
{
	int x;
	int y;
	int z;
	int res;
	char cont;
	int yes;
	
	do
	{
		cout<<"MENU:\n\t1. Add\n\t2. Subtract\n\t3. Multiply\n\t4. Divide\n\t5. Modulus\nEnter you choice: ";
		cin >> x;
		
		if(x==1)
		{
			cout<<"Enter your two numbers: ";
			cin >> y;
			cin >> z;
			res = y + z;
			cout<<"Answer: "<< res;
		}
		else if(x==2)
		{
			cout<<"Enter your two numbers: ";
			cin >> y;
			cin >> z;
			res = y - z;
			cout<<"Answer: "<< res;
		}
		else if(x==3)
		{
			cout<<"Enter your two numbers: ";
			cin >> y;
			cin >> z;
			res = y * z;
			cout<<"Answer: "<< res;
		}
		else if(x==4)
		{
			cout<<"Enter your two numbers: ";
			cin >> y;
			cin >> z;
			res = y / z;
			cout<<"Answer: "<< res;
		}
		else if(x==5)
		{
			cout<<"Enter your two numbers: ";
			cin >> y;
			cin >> z;
			res = y % z;
			cout<<"Answer: "<< res;
		}
		else
			cout<<"Invalid Input!";
			
		cout<<"\nPress 'y' to continue!";
		
		cin>> cont;
		if(cont != 'y')
		{
			cout<<"Goodbye!";
			return 0;
		}
		else
			yes = 1;
		
	}while(yes == 1);
	
	return 0;
}
